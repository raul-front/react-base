import React, { FC } from 'react'
import './index.less'

interface IProps {
}

const Header: FC = (props: IProps) => {
  return (
    <div className="header">
      Header
    </div>
  )
}

export default Header
