import React, { FC } from 'react'
import './index.less'

interface IProps {
}

const Com: FC = (props: IProps) => {
  return (
    <div className="com">
      Com
    </div>
  )
}

export default Com
